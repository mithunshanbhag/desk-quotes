name: desk-quotes-deploy
run-name: deploy desk-quotes

on:
  push:
    branches:
      - main
    # do not trigger on changes to documentation
    paths-ignore:
      - 'docs/**'
      - '**/*.md'
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy-template.yml
    secrets: inherit
